extends layout

block content

  
  h1.form= title
  p.form
  p.form ♊✨ ☪🚀⛩✨ 🍉🚀⛰
  p.form "We Love You"

  if detected



  else

  p.form Encode
  form.form(oninput='encode()')
    input.form(type='text', name='form')
    |  : 
    output.form(name='output', for='form', form='foo' onoutput='encode()')








